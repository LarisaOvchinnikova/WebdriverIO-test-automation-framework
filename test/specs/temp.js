import { expect } from 'chai';

const url = '';

const selector = {
    inputField: '',
};
const expected = {
    h1: '',
};
const data = {
    search: '',
};

describe('', () => {
    before(() => {
        browser.url(url);
    });

    it('', () => {

    });
});
